/** Max length for ingest blocklist `pattern` (and API validation). */
export const INGEST_BLOCKLIST_PATTERN_MAX = 500;

/** Max length for optional admin `note`. */
export const INGEST_BLOCKLIST_NOTE_MAX = 500;
